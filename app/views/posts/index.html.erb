<h1>Instagram</h1>
<% @posts.each do |post| %>
    <%=post.id%>번째 글 :<!-- <img src="<%=post.image_url%>"> <%=link_to "보기", post_path(post)%>
   -->
    <%= link_to image_tag(post.image_url), post_path(post)%>
     <br>
<% end %>

<%= link_to "새 글 등록", new_post_path %>